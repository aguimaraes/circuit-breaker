machine:
  php:
    version: 7.0.17
    timezone:
        Europe/Berlin

dependencies:
  override:
    - composer install --no-interaction

## Customize test commands
test:
  override:
    - vendor/bin/phpunit
