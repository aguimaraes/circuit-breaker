machine:

  timezone:
    Europe/Berlin

  # Version of php to use
  php:
    version:
      7.0.17

  override:
    - composer install --no-interaction:
       pwd:
         circuit-breaker

## Customize test commands
test:
  override:
    - vendor/bin/phpunit
