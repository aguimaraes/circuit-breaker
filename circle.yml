machine:
  php:
    version: 7.0.17
    timezone:
        Europe/Berlin

dependencies:
  override:
    - composer install --no-interaction:
       pwd:
         circuit-breaker

## Customize test commands
test:
  override:
    - vendor/bin/phpunit
