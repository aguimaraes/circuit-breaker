machine:
  php:
    version: 5.6.17
    timezone:
        Europe/Berlin

dependencies:
  override:
    - sudo apt-get install php5-apcu
    - composer install --no-interaction

## Customize test commands
test:
  override:
    - vendor/bin/phpunit
