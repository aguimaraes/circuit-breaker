machine:
  php:
    version: 7.1.3
    timezone:
        Europe/Berlin

dependencies:
  override:
    - composer install --no-interaction

## Customize test commands
test:
  override:
    - vendor/bin/phpunit
